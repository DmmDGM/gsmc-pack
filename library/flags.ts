// Defines flags
export interface Flags {
    "client-side":      boolean;
    "dot-minecraft":    boolean;
    "force-sync":       boolean;
    "pack-directory":   string;
    "pack-file":        string;
    "server-side":      boolean;    
}

// Exports
export default Flags;
