// Defines code enum
export enum Code {
    DEP_ACTION_OKAY,
    DEP_ORIGIN_SKIPPED,
    SYNC_ACTION_OKAY,
    SYNC_FILE_EXISTS,
    SYNC_ORIGIN_SKIPPED,
    TEST_ACTION_OKAY,
    TEST_ORIGIN_SKIPPED,
    TEST_RESOURCE_INVALID
}

// Exports
export default Code;
